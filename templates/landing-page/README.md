# Template: Landing Page

A Vue 3 + Vite SPA. Use this for landing pages, marketing sites, or tools that don't need a custom backend.

**Tier 1** — client only, no server.

## Stack

Vue 3 (Composition API) + Vite + Pinia + Vue Router + TypeScript

## Usage

```sh
pnpm dev        # dev server at localhost:5173
pnpm build      # production build to dist/
pnpm test       # run tests
```

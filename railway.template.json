{
  "_comment": "Template railway.json for deploying apps from this monorepo",
  "_instructions": "1. Copy this file AND nixpacks.toml to apps/<your-app>/, 2. Update APP_NAME in nixpacks.toml if needed, 3. Adjust buildCommand if your app structure differs",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm run db:generate && pnpm run build",
    "watchPatterns": ["apps/APP_NAME/**", "packages/**"]
  },
  "deploy": {
    "startCommand": "pnpm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
